/**
 * App-wide constants. No user/workspace fallbacks — those come from API.
 */

export const APP_NAME = "RepoLens";

/** Default plan label in UI when API has not returned yet */
export const WORKSPACE_PLAN = "Hobby";
